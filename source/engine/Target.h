#pragma once

namespace herald {

/// The base for any thing that the CPU can
/// render audio and visual data to.
class CpuTarget {
public:
  /// Just a stub.
  virtual ~CpuTarget() {}
};

} // namespace herald
