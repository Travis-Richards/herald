dist: xenial

sudo: false

os:
  - linux
  - osx

addons:
  apt:
    packages:
      - qt5-default
  homebrew:
    packages:
      - qt5

language: cpp

compiler: clang

script: scripts/travis-build.sh
